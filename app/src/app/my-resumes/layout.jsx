export const metadata = {
    title: "My Resumes Dashboard | CVGrid",
    description: "Manage your saved resumes, create duplicates, edit content, and view downloads.",
    robots: {
        index: false,
        follow: false,
    }
};

export default function MyResumesLayout({ children }) {
    return <>{children}</>;
}
