export const metadata = {
    title: "Resume Preview – Review & Download Your CV | CVGrid",
    description: "Preview your resume template, select languages, and download in PDF, Word, or PNG formats.",
    robots: {
        index: false,
        follow: false,
    }
};

export default function PreviewLayout({ children }) {
    return <>{children}</>;
}
