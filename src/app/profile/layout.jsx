export const metadata = {
    title: "My Profile & Settings | CVGrid",
    description: "Manage your CVGrid account settings, profile information, and subscription details.",
    robots: {
        index: false,
        follow: false,
    }
};

export default function ProfileLayout({ children }) {
    return <>{children}</>;
}
